# 2026-08-08 情节恢复与支线扩充记录

用户反馈：情节缺漏太多、之前补充的情节（含数学解题式教学）不见了、支线剧情太少。

## 原因定位

1. 上一轮为避免“角色模板覆盖手写文案”，主线节点改为只显示 base 文案，导致玩家看不到角色专属情节措辞。
2. 支线剧情弧有资源门槛（信任/影响力 45），新玩家容易看不到支线内容；锁定原因只显示“前置未解锁”。
3. 数学公式、应用要点、举一反三例题只在训练完成后的结果页展示，玩家在决策前没有“先教学”的入口。

## 本轮处理

- `src/core/story.ts`：`getNodeForRole` 恢复角色专属选项文案，主线反馈同时保留手写判断依据与理论引用。
- 新增第三条支线弧 `power_boundaries`（权力边界）：`s7` 越级汇报、`s8` 绕过决策、`s9` 交接断点，各含 expert/partial/risk 三选项与完整反馈/理论。
- `src/core/translations.ts`：新增 `SIDE_NODE_EN` s7/s8/s9 与 `SIDE_ARC_EN.power_boundaries`，中英双语齐全。
- `src/ui/App.ts`：支线资源门槛 45 → 30；地图支线节点显示具体解锁原因；主线地图支线计数改为动态。
- 训练页：测验前新增“公式 / 应用要点 / 举一反三例题”教学面板。
- 能力图谱：每张能力卡直接显示公式表达式与示例场景。

## 验证

- `npm run build` PASS
- `npm run test:unit` PASS
- `npm run content-audit` PASS（74 剧情节点 / 222 选项 / 9 支线节点，中英全覆盖）
- `npm run i18n-audit` PASS（status: full）
- `npm test` PASS
- `npm run audit` PASS（桌面/手机中英文 0 溢出）
- `npm run test:features` PASS
- `npm run test:i18n-browser` PASS
