# 权变之路 UI/UX Audit

## 已检查

- 桌面 1280x720：无横向溢出，主菜单、地图、剧情、1v1 均可操作
- 手机 390x844：无横向溢出，单列布局，按钮触控高度不小于 44px
- 全页面审计：主页、地图、剧情、能力图谱、复盘报告、1v1 均无溢出与控制台错误
- 主线地图章节导航在窄屏可横向滑动
- 远程 1v1 邀请码输入与复制区域在窄屏改为单列
- 文本使用系统中文黑体，卡片内文本换行，不依赖固定字号缩放
- 已加入程序化“权力关系沙盘”与情境局势图，补充核心视觉资产
- 程序化权力关系沙盘已迭代：渐变背景、中心光晕、节点发光与连接质感
- 已加入 Web Audio 程序化音效、氛围声与静音开关
- Web Audio 已迭代：三音低音垫、循环旋律与更完整的音效层
- 能力基线测评页新增“能力基线图”程序化视觉
- 本地双人结算已补回合音效，测评页已补静音开关
- 报告页云端同步/载入/排行榜已补连接、成功、失败音效与状态视觉反馈
- 已补移动端安全区、焦点可见样式、读屏状态与中英核心界面切换
- 已补键盘快捷键 H/M/A/R/D、菜单快捷键标注、结果面板读屏播报与语言切换 `html lang`
- 所有 `main` 视图已补双语 `aria-label`
- `npm run audit` 已自动检查按钮可读名称、canvas `aria-label`、`main` 地标与 `main` 标签
- 已完成节点渲染为禁用状态，核心结算层也拒绝重复结算，封死刷资源路径
- 每日挑战按 `claimedDaily[YYYY-MM-DD]` 领取，跨天自动重置
- 地图新增“事件簿”，展示 20 个随机事件完成进度
- 成就墙新增进度条与 `current / target` 提示
- 本地存档写失败会提示导出，损坏存档会尝试 session 备份恢复并提示
- 远程 WebRTC 支持通过 `VITE_TURN_URL` / `VITE_TURN_USERNAME` / `VITE_TURN_CREDENTIAL` 注入 TURN
- 1v1 计分加入 trust / influence / capital 资源加成，不再只有精力参与
- 新增 localStorage 匿名事件日志（建档、章节决策、每日领取、训练、试炼、对决、重置）
- 新增 `npm run test:accessibility`：axe-core 全页审计，桌面/手机 8 个视图 0 严重违规

## 当前问题

- P2：WebRTC 手动信令流程对首次用户有理解成本，需要加一步可视化连接状态
- P2：远程连接尚未在真实公网双端完成验证（TURN 已可配置，仍需部署后实测）
- P3：真机安全区、手势条与刘海屏截图验证（自动矩阵已覆盖 360x800 / 720x1280 / 1080x2400 / 平板 / 横屏 / 1.5x 字体）
- P3：仍待真机 VoiceOver / TalkBack 实测
- P3：本地存档无跨设备同步

## 下一步

1. 部署服务端后完成公网双端 WebRTC 与云端闭环测试
2. 在真机完成 VoiceOver / TalkBack 与刘海/手势条验收
3. 把主线序列改为受决策画像影响的轻量分叉
