# 2026-08-08 静态版内容保留记录

用户要求：保持静态版，但之前写好的相关内容也要放进静态版。

## 做法

- `src/styles.css`：静态版不再用 `display:none` 隐藏 `.online-only`，改为锁定态展示（半透明 + 不可点击），云端内容在静态版可见。
- `src/ui/App.ts`：
  - 报告页云端区域前增加静态版说明；账号/云存档/排行榜/自动匹配以锁定态展示。
  - 1v1 大厅云端自动匹配区域增加说明，手动邀请码远程对战仍离线可用。
  - 新增本地排行榜：最佳对局分 + 最近 5 场对战记录。
  - 设置页“关于”补充静态版内容清单。
- 代码层面 server/index.mjs、roomClient.ts 与云端功能未删除，仅由构建开关控制在在线版启用。

## 验证

- `npm run build` PASS
- `npm run test:unit` PASS
- `npm run content-audit` PASS（74 剧情节点 / 222 选项 / 9 支线）
- `npm run i18n-audit` PASS
- `npm test` PASS
- `npm run audit` PASS（桌面/手机中英文 0 溢出）
- `npm run test:features` PASS
- 静态产物确认包含：锁定说明、本地排行榜、云端自动匹配说明。
