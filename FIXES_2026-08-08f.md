# 2026-08-08 20 项完整性验证 · 逐项处理记录

处理前按要求在 GitHub 检索同类项目：未找到可直接照搬的多存档槽开源实现，参照了 CRPG 多档案、roguelike 角色档案与 `coderogue` / `masterylearning` / `CTAT` 的设计原则（独立槽位、全局进度、失败可重来）。以下为本轮逐项核对结果。

| # | 问题 | 状态 | 落地 |
| --- | --- | --- | --- |
| 1 | 专家选项恒为首选 | ✅ 已修复 | `storyOptionOrder` 按节点/角色/局数种子洗牌展示顺序 |
| 2 | 角色模板覆盖手写文案 | ✅ 已修复 | 主线保留角色措辞并叠加手写判断依据与理论引用 |
| 3 | AI 恒选专家、难度无效 | ✅ 已修复 | `aiPick` 按强度 0.45/0.65/0.85 选专家；AI 对手按玩家能力生成 |
| 4 | 单存档槽换角色必删档 | ✅ 本轮修复 | 三角色独立存档槽 + 当前角色指针 + 旧档自动迁移 + 角色档案切换 UI |
| 5 | 首章三星拿不到 | ✅ 已修复 | 三星门槛 200，首章专家满分可达成 |
| 6 | risk 白送 capital | ✅ 已修复 | risk 改为扣 1 capital，expert 给 trust/influence |
| 7 | 最差也能通关 | ✅ 已修复 | 一星门槛 100 分解锁下一章，未达可重打 |
| 8 | 能力经验可刷 | ✅ 已修复 | 已完成节点拒绝再次结算 |
| 9 | 门槛不触发 | ✅ 已修复 | 第 2-4/5-7/8-9 章分别需 2/3/4 级并灰显 |
| 10 | 能力成长速度差 5 倍 | ✅ 已缓解 | 10 条训练全覆盖 + 推荐训练 + 角色路线；配平可继续迭代 |
| 11 | 1v1 永久挂起 | ✅ 已修复 | 60 秒超时强制落子并结算 |
| 12 | 存档写入无捕获、历史无上限 | ✅ 已修复 | `saveState` 捕获失败并提示；历史各保留 200 条 |
| 13 | 标准档 30s 倒计时 | ✅ 已修复 | `NORMAL_DECISION_MS = 0` |
| 14 | 训练/修炼二次零收益 | ✅ 已修复 | 复训恢复精力与掌握分；修炼可重复获小额精力 |
| 15 | 章节复盘拼进每个反馈 | ✅ 已修复 | 复盘只出现在章节过渡与地图 |
| 16 | branch 模板机械展开 | ✅ 已缓解 | 角色专属措辞 + 角色路线；内容扩展可继续迭代 |
| 17 | 支线顺序未强制 | ✅ 已修复 | 支线节点按弧顺序解锁并显示具体原因 |
| 18 | 两个“精力”命名冲突 | ✅ 已修复 | “剧情精力 / 试炼精力” |
| 19 | 品牌名三套并存 | ✅ 已修复 | 页面/PRD 统一“升维 · Ascend”，存档 key 作为技术标识保留 |
| 20 | 导入存档死按钮 | ✅ 已修复 | handler 已接，触发隐藏文件输入 |

## 本轮新增（P0-4 多槽存档）

- `src/core/game.ts`：
  - `roleSaveKey(role)` / `ROLE_IDS`：三角色独立槽位。
  - `loadSave(role?)`：按当前角色指针读取，无槽位回退旧档并自动迁移。
  - `saveState`：建档后同时写入角色槽与当前角色指针。
  - `roleSlotSummaries()` / `deleteRoleSlot(role)`：槽位列表与单槽删除。
- `src/ui/App.ts`：
  - 主菜单新增“角色档案”入口。
  - 建档页新增角色档案切换面板（切换 / 新建 / 当前标记）。
  - 重置只删除当前角色槽，不再清空其他角色。
- `scripts/unit-test.mjs`：三角色独立存档、切换、删除回归测试。

## GitHub 参照说明

本轮未找到成熟的多存档槽开源项目，参照了通用 CRPG/roguelike 多档案设计与既有 `coderogue`、`masterylearning`、`CTAT` 的“独立进度 + 全局循环 + 失败重来”原则，并在文档中如实记录。

## 验证

- `npm run build` PASS
- `npm run test:unit` PASS（含多角色存档槽回归）
- `npm run role-audit` PASS（0 问题）
- `npm run leadership-audit` PASS（0 问题）
- `npm run content-audit` PASS
- `npm run i18n-audit` PASS
- `npm test` PASS
- `npm run audit` PASS
- `npm run test:features` PASS
