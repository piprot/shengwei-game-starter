# 2026-08-08 第二轮完整性修复记录（静态版）

处理依据：附件报告「升维 / Ascend 静态版 综合 6.0/10」中的 20 条短板 + 首轮报告仍存在的问题。
处理前已按要求在 GitHub 检索同类项目并参考：`dfabulich/choicescript`、`hpi-sam/fuesim-digital`、`trinicy-Col/competing-leaders-full`、`pymc-labs/decision-lab`。

| # | 问题 | 落地 |
| --- | --- | --- |
| 1 | sw.js 缓存名固定且缓存优先 | `public/sw.js` 改为 v2 并区分 shell/assets 缓存；构建后 `scripts/stamp-sw.mjs` 注入时间戳版本号；JS/CSS 用 stale-while-revalidate，导航用 network-first |
| 2 | 静态版远程对战只有 STUN | 未配置 `VITE_TURN_URL` 时远程大厅显示「实验性」提示；TURN 环境变量注入保留 |
| 3 | 分析事件只写 localStorage | 设置页新增「导出事件日志」，`exportAnalytics()` 下载事件 JSON |
| 4 | 本地存档清缓存即丢 | 菜单新增首次建档备份提示（导出/复制链接/知道了）；报告页常驻提醒 |
| 5 | 无正式美术资产 | 新增 `public/art/chapter-1..9.svg` 九张章节背景，接入地图与剧情页场景化布局 |
| 6 | 无正式音乐 | `GameAudio` 增加菜单/剧情/对局三套 BGM（`setAmbientScene`），随视图切换 |
| 7 | 选择反馈只有文字数字 | 结算面板新增资源条与人物关系图（`outcome-resources` + `renderRelationGraph`），保留分数动效 |
| 8 | 缺少新手引导 | 首剧情节点已有三步引导；菜单在 `profileCreated && playCount === 0` 时新增三步开局指南 |
| 9 | 资源系统只有门槛加减 | 新增 `applyDailyResourceRecovery` 每日资源恢复；地图显示恢复横幅；低资源在结算条高亮 |
| 10 | 平衡数值未校准 | 新增 `npm run balance-sim` 蒙特卡洛脚本；星级门槛改为 200/150/100 一星制 |
| 11 | AI 公式粗糙、恒选专家 | `buildAiProfile` 基于玩家能力分布生成稳定对手；`aiPick` 按强度 0.45/0.65/0.85 选专家 |
| 12 | 对局刷新丢失 | 新增对局快照 `localStorage` + 大厅「继续上次对局」按钮；AI/本地模式每步持久化 |
| 13 | 邀请码长 SDP | 复制按钮已有，远程大厅标注实验性；保留剪贴板分享路径 |
| 14 | 随机事件固定 20 且消失 | 新增 `rotateRandomEventPool`，全部完成后可「轮转事件池」重新可接 |
| 15 | 内容撑不起长期运营 | 新增周常挑战（`weeklyChallenges` + 重置倒计时）与无尽挑战入口 |
| 16 | 试炼/训练耦合弱 | 保持能力门槛可见，新增恢复/重复训练小奖励，周常与无尽挑战补充成长路径 |
| 17 | 每日挑战不突出 | 地图固定展示今日 3 任务并新增每周挑战面板与倒计时 |
| 18 | UI 面板卡片堆叠 | 章节背景图作为场景化底色，地图/剧情页改为全幅场景布局 |
| 19 | 无失败风险 | 星级门槛 100 分解锁下一章；未达一星可「重新挑战本章」；风险选项不再白送组织资源 |
| 20 | 发布包装缺失 | 设置页新增版本与「关于升维」面板；版本号 `1.1.0`；`CHANGELOG.md` 记录更新 |

## 核心逻辑修复（首轮报告遗留）

- 标准档不再计时：`NORMAL_DECISION_MS = 0`，与「标准档无压力」承诺一致。
- 专家选项不再恒为首选：`storyOptionOrder` 按节点/角色/局数种子洗牌展示顺序，结算仍使用原始索引。
- 主线手写文案不再被角色哈希模板覆盖：`getNodeForRole` 对主线节点保留 base 选项，角色模板仅用于分支/随机节点；章节复盘不再拼进每个选项反馈。
- 能力门槛真实生效：第 2-4 章需 2 级、5-7 章需 3 级、8-9 章需 4 级，UI 灰显并显示缺口。
- 存档历史上限：`decisionHistory` / `duelHistory` 各保留最近 200 条。
- 本地 1v1 不再永久挂起：`DUEL_ROUND_TIMEOUT_MS = 60s`，超时强制放入风险回合并结算。
- 重复训练不再零收益：复训恢复试炼精力并给 1 点掌握分；修炼任务可重复并获得小额精力。
- 两个「精力」命名区分：「剧情精力」与「试炼精力」。
- 品牌名统一：页面/README/PRD 使用「升维 · Ascend」，存档 key 作为技术标识保留。
- 设置页「导入存档」死按钮已接 handler：点击后触发隐藏文件输入。

## 验证结果（2026-08-08 第二轮）

- `npm run build` PASS（tsc + vite + service worker 版本戳）
- `npm run test:unit` PASS（含星级门槛、重打、经济闭合、恢复循环、事件轮转、对局快照回归）
- `npm test` PASS（端到端建档→情境→分支→1v1 冒烟）
- `npm run audit` PASS（桌面/手机中英文 0 溢出、0 页面错误）
- `npm run test:features` PASS
- `npm run content-audit` PASS（71 节点 / 213 选项 / 27 角色分支全量）
- `npm run i18n-audit` PASS
- `npm run test:i18n-browser` PASS
- `npm run test:device-screenshots` PASS（18 个视口 / 语言 / 字体组合）
- `npm run test:accessibility` PASS（axe 桌面/手机 8 视图 0 违规）
- `npm run test:rtc` PASS（本地双浏览器 WebRTC）
- `npm run test:server` / `test:server-edge` PASS
- `npm run deploy:check` PASS
- `npm run balance-sim -- 300` 输出：随机全流程 300 局，章节通过率约 66%-77%，三星约 2%-11%，符合「最差答案不能通关、认真决策可达成」的目标

## 发布检查

- 静态产物 `dist/` 不含 Railway URL，含 `online-off` 静态版标记、九张章节美术与版本化 Service Worker。
- GitHub Pages 部署由 `.github/workflows/ci.yml` 的 `deploy` job 自动完成。
