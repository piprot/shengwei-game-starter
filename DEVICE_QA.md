# 真机与无障碍验收清单

## 目标

在真实设备上验证移动端安全区、触控热区、字体可读性与读屏体验。

## 真机矩阵

- iPhone 15 / 720x1280 等效视口
- Android 1080x2400 等效视口
- 窄屏 360x800
- 平板 1024x768
- 横屏 844x390
- 系统字体 1.5x（自动矩阵已覆盖）

## 移动端检查项

- 无横向滚动
- 底部导航不被手势条遮挡
- 顶部内容不被刘海/状态栏遮挡
- 按钮触控高度不小于 44px
- 长文本不溢出
- 竖屏与横屏均可完成一局

## 无障碍检查项

- 每个按钮都有可读文本或 `aria-label`
- 状态变化通过 `aria-live` 播报
- 焦点顺序符合视觉顺序
- `focus-visible` 样式清晰
- 对话/选项内容可通过 VoiceOver / TalkBack 阅读
- 语言切换后 `html lang` 正确更新

## 当前自动化覆盖

- `npm run audit`：桌面/手机无溢出、无控制台错误
- `npm run test:device-screenshots`：360x800 / 720x1280 / 1080x2400 / 1024x768 / 844x390 横屏，中英文、1.5x 字体菜单/地图/剧情/报告截图与溢出检查
- `npm run test:accessibility`：axe-core 桌面/手机 8 视图无障碍审计，0 严重违规
- `npm run test:rtc`：两个本地浏览器页面完成 WebRTC 远程对局
- `npm run test:server`：服务端端到端
- `npm run content-audit`：内容完整性

## 人工验收动作

1. 新建档案并完成 30 题测评
2. 完成第一章首个主线 + 角色分岔
3. 完成一个支线剧情弧节点
4. 完成一个随机事件
5. 完成一场 AI 1v1
6. 打开能力图谱、复盘报告、成就墙、人物关系图
7. 导出报告并复制存档链接
8. 云端同步/载入/排行榜（需服务端公网部署后）
9. 中文/英文切换后复查关键页面
